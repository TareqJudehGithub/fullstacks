Aggregate Functions

 DISTINCT
  - Removed Duplicate data

 GROUP BY
  - Removed Duplicate data
  - Never GROUP BY a unique columns (like Id)
  - We have EVERY column we SELECT in the GROUP BY

 COUNT
  - Counts total items (cells) in a column

 Avg
  - 
 SUM

 HAVING
  -HAVING is used instead of WHERE but for aggregates


